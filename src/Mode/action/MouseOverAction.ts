import ModeAction from "../ModeAction";

export default class MouseOverAction extends ModeAction
{
    public static NAME = 'mouse-over-action-mode-layout-creator'


    public getName() {
        return MouseOverAction.NAME

    }
}
