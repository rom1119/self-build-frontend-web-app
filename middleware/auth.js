export default function asd({$auth}) {

    console.log('działą')
    // console.log($auth)
    // if(!isAuth()) {
    //     // return ctx.redirect('/login')
    // }
}
function isAuth(ctx) {
    // Check if user session exists somehow
    return false;
}
